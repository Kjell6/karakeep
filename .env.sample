# See https://docs.karakeep.app/configuration for more information
DATA_DIR=<path>
NEXTAUTH_SECRET=<secret>

# Optional: full-text search. ./start-dev.sh sets this automatically if unset
# when Docker Meilisearch is used on port 7700.
# MEILI_ADDR=http://127.0.0.1:7700